# Windows 平台专属依赖 (仅 Windows 需要安装)
uiautomation>=2.0
pywin32>=303

# ── OCR 识别库 (WebOCR 功能) ──
# 微信 OCR 运行时 (含 WeChatOCR/ 目录 DLL 二进制, 已由 recorder.spec 打包)
# 注意: wechat_ocr 无 PyPI dist-info, 若 pip 安装失败请从本地 .whl/源码手动安装
#wechat-ocr
# RapidOCR + OpenVINO 回退引擎 (需与 .venv 中版本一致, 否则模型/二进制路径需重新收集)
rapidocr-openvino==1.4.4
# RapidOCR 运行时依赖 (OpenVINO / 裁剪算法 / protobuf)
openvino==2024.0.0
pyclipper==1.4.0
protobuf>=4.0
