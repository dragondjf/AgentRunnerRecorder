# 通用依赖 (跨平台)
pillow>=8.0
opencv-python-headless>=4.5
numpy>=1.21
mss>=0.5
pynput>=1.7
requests>=2.28
requests-toolbelt>=1.0.0
loguru>=0.7
flask>=2.0
flask-cors>=3.0
psutil>=5.9
python-dotenv>=1.0


# urecorder 依赖 (Flask 服务启动必需)
httpx>=0.24
pyyaml>=6.0

# recorder 桌面端依赖
rich>=13.0
python-docx>=0.8.11

# recorder / urecorder 通用跨平台 GUI 自动化依赖
pyautogui>=0.9

# urecorder Word 导出依赖
pypandoc_binary>=1.13

# urecorder Qwen VL 蓝图依赖 (autogen_ext 传递依赖)
openai>=1.0
tiktoken>=0.5

# urecorder Qwen VL 蓝图依赖 (可选, 如不需要 VL 功能可不安装)
autogen-agentchat>=0.4
autogen-core>=0.4
autogen-ext>=0.4

# urecorder postcase 导出依赖
pandas>=1.3
xlsxwriter>=3.0
PyPDF2>=3.0
pdfplumber>=0.10

# Flask 传递依赖 (显式声明以锁定版本)
werkzeug>=2.0

# 构建依赖 (Cython 编译 / PyInstaller 打包时需要)
setuptools>=65
Cython>=3.0

# Windows 用户请额外安装 requirements-windows.txt:
#   pip install -r requirements.txt -r requirements-windows.txt



